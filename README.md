isucon4 with luajit + nginx + openresty + redis
==================================================

# Quick start

```sh
$ sudo yum install pcre-devel
$ sudo yum install redis
$ make install
$ make start-redis
$ make start
$ curl localhost:8080
```

