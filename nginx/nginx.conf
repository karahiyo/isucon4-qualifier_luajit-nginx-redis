worker_processes  1;

env             PWD;

error_log       logs/error.log;
pid             run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include         mime.types;
    default_type    application/octet-stream;

    access_log      logs/access.log;

    sendfile        on;
    keepalive_timeout 30;

    server {
        listen      8080;
        server_name localhost;

        access_log  logs/app.access.log;

        location / {
            content_by_lua 'ngx.say("index")';
        }

        location /login {
            content_by_lua 'ngx.say("login")';
        }

        location /mypage {
            content_by_lua 'ngx.say("mypage")';
        }
        location /report {
            content_by_lua 'ngx.say("report")';
        }
    }
}
